import { TruncTitlePipe } from './trunc-title.pipe';

describe('TruncTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TruncTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
