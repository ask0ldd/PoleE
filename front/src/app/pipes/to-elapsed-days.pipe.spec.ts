import { ToElapsedDaysPipe } from './to-elapsed-days.pipe';

describe('ToElapsedDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new ToElapsedDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
